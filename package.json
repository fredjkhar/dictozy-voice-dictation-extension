{
  "name": "dictozy-voice-dictation-extension",
  "version": "0.1.9",
  "private": true,
  "scripts": {
    "test": "node --test extension/tests/*.test.js",
    "test:browser": "playwright test",
    "test:site": "playwright test --config=playwright.site.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
